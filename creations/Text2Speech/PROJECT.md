# Text to Speech Converter

A simple command-line tool that converts text to speech using Python. This project is small, fully buildable in a single session, and uses only Python. It has a clear definition of "done" — the CLI should correctly convert sample text inputs to speech. This tool is something I can verify myself by running it, ensuring it meets the specified criteria.